{
  "name": "@openanalytics/integrations",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Stripe, revenue provider, email and import adapters behind normalized contracts.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --build tsconfig.json",
    "clean": "tsc --build tsconfig.json --clean"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1092.0",
    "@aws-sdk/s3-request-presigner": "^3.1092.0",
    "@openanalytics/domain": "workspace:*",
    "nodemailer": "9.0.5"
  }
}
