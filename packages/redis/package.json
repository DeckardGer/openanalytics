{
  "name": "@openanalytics/redis",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Durable queue, realtime and cache adapters behind a provider-agnostic contract.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --build tsconfig.json",
    "clean": "tsc --build tsconfig.json --clean"
  },
  "dependencies": {
    "@openanalytics/domain": "workspace:*",
    "@openanalytics/observability": "workspace:*",
    "ioredis": "5.11.1"
  }
}
