{
  "name": "@openanalytics/domain",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Pure business rules: plans, entitlement, usage windows, policy config invariants.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --build tsconfig.json",
    "clean": "tsc --build tsconfig.json --clean"
  },
  "dependencies": {
    "@openanalytics/contracts": "workspace:*",
    "zod": "4.4.3"
  }
}
