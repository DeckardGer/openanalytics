# valkey-queue — the durable event queue.
#
# The same password appears inside EVENT_STREAM_REDIS_URL in collector.env and
# worker.env. AUTH is not optional here: the client factory refuses a queue URL
# without credentials, because the credential is what keeps a neighbouring
# container from becoming a queue writer.
VALKEY_PASSWORD={{VALKEY_QUEUE_PASSWORD}}
