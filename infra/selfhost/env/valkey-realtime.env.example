# valkey-realtime — presence, rate-limit counters, short-lived caches.
#
# The same password appears inside REALTIME_CACHE_REDIS_URL in api.env,
# collector.env, worker.env, gateway.env and realtime.env.
VALKEY_PASSWORD={{VALKEY_REALTIME_PASSWORD}}
